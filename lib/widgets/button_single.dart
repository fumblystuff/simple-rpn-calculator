import 'package:flutter/material.dart';

class ButtonSingle extends StatelessWidget {
  const ButtonSingle({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
