import 'package:flutter/material.dart';

class ButtonDouble extends StatelessWidget {
  const ButtonDouble({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
